<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartlibrary.dao.CrossAnalysisDao">
    <select id="CrossAnalysisByDay"  resultType = "CrossAnalysis" >
        SELECT HOUR,book_lend_times,croom_times,eread_times,seat_times,print_times,gctrl_times
        FROM  CrossAnalysisByDay
    </select>
    <select id="getCrossAnalysisByMonth" resultType = "CrossAnalysis">
        SELECT month,book_lend_times, croom_times, eread_times,seat_times, print_times,gctrl_times
        FROM zz_time_month_count
        WHERE IF(MONTH(NOW()) > 9 , `year` = YEAR(NOW()),`year` = YEAR(NOW()) - 1)
    </select>
    <select id="getCrossAnalysisByAcademyInUnderGraduate" resultType = "CrossAnalysis">
        SELECT * FROM crossanalysisbyacademyundergraduate
    </select>
    <select id="getCrossAnalysisByAcademyInGraduate" resultType = "CrossAnalysis" >
        SELECT * FROM crossanalysisbyacademygraduate
    </select>
    <select id="getCrossAnalysisUtilizationRatio" resultType = "CrossAnalysis" >
        SELECT * FROM crossanalysisutilizationratio
    </select>
</mapper>