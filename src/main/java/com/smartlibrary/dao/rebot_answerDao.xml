<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartlibrary.dao.rebot_answerDao">
    <select id="getNumber" resultType="String" parameterType="String">
        SELECT count FROM zz_bigscreen_num WHERE `name` = #{name}
    </select>
</mapper>