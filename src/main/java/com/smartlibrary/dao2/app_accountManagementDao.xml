<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smartlibrary.dao2.app_accountManagementDao">
    <insert id="add" parameterType="com.smartlibrary.domain2.app_accountManagement" >
      INSERT INTO z_account (account, password, type, permision_allocate, school_id)
VALUES
      (#{account},#{password},1,#{permission_allocate},1)
    </insert>
    <select  id="showList" parameterType="com.smartlibrary.domain2.app_accountManagement">
        SELECT account, type, school_id  FROM z_account  WHERE type = 1 LIMIT #{offset},#{pageSize}
    </select>
    <select id="count" resultType="Integer" parameterType="com.smartlibrary.domain2.app_accountManagement">
        select count(*)
        from app_employee
        WHERE type = 1
    </select>
</mapper>